kind: Service
apiVersion: v1
metadata:
  name: cloud-app-service
  namespace: cloud-app-ns
spec:
  type: NodePort
  selector:
    app: cloud-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30007
